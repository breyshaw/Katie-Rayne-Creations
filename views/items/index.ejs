<%- include('../partials/html-head') %>


<main>
    <img class="animate__animated animate__heartBeat" id='logo' src='https://i.imgur.com/nj8Wsml.png' alt="logo"/>
    <%- include('../partials/nav') %>
    <h1 class='animate__animated animate__backInRight' >All Items</h1>
    <section class='animate__animated animate__backInRight' >
            <% items.forEach((item) => { %> 
                    <div>
                    <p> <%= item.title %> </p>
                    <a href="/items/<%= item._id %> ">
                    <img src= "<%= item.imageUrl %>" alt="Image of Item" class="item-img">
                    </a>
                    </div>
                    <a href="/items/<%= item._id %> ">
                      <button type="button" class="btn btn-secondary">Details</button>
                      </a>
                    <div>
                    </a>
                    <% if (user) { %> 
                    <% if (user.isAdmin) { %> 
                    <div>
                    <a href="/items/<%= item._id %>/edit">
                        <button class='btn btn-success'>Edit Item</button>
                      </a>
                    </div>
                    <form action="/items/<%= item._id %>?_method=DELETE" method="POST">
                    <button class="btn btn-danger">Delete item</button>
                    </form>
                    <% } %>
                    <% } %>
                <% }) %> 
    </section>
</main>

<%- include('../partials/footer') %>